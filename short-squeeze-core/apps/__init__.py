"""Applications built on top of the ``squeeze_core`` runtime.

Packages here are view/controller layers. They may read runtime contracts and local
tooling, but the runtime never imports them: ``src/squeeze_core`` stays a closed,
additively-versioned research core.
"""
