# ADR 0060: Outcome artifacts remain separate

## Decision

Outcome artifacts use a manifest distinct from discovery and evaluation inputs and are captured only after the Phase 3A request and result freeze.

## Consequences

The leakage audit can prove ordering and block publication when separation fails.
