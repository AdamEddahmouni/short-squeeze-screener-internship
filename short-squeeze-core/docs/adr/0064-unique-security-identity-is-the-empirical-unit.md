# ADR 0064: Unique security identity is the empirical unit

## Decision

The default independent empirical unit is a resolved security identity, not a symbol string or boundary count. Earliest eligible boundary is primary.

## Consequences

Repeated BIYA boundaries remain dependent observations and do not inflate independent sample size.
