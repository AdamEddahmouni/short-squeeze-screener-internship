# ADR 0045: Rule outcomes remain independent

Accepted. Phase 3A emits exactly one outcome for each explicitly enabled rule and never
derives an overall candidate outcome. Category summaries contain counts only. This
preserves evidence gaps and prevents an attractive result in one domain from masking an
unknown or conflicted result in another.

