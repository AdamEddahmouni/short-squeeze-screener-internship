# ADR 0046: Momentum discovery is not short-pressure confirmation

Accepted. Price, return, volume, and float activity belongs exclusively to
`MOMENTUM_DISCOVERY`. Published short interest, days to cover, and borrow evidence
belongs exclusively to `SHORT_PRESSURE_CONFIRMATION`. Phase 3A has no mechanism that
collapses the categories into a squeeze claim.

